version: 2
updates:
  # Keep the GitHub Actions used by the quality workflow current.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
